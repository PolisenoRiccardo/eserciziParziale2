#include <stdio.h>
#include <stdlib.h>

typedef struct nodo {
  int valore;
  struct nodo* next;
} nodo;

Nodo* creaNodo(int valore) {
  
}

int main(void) {
  Nodo *head = NULL;
  Nodo *tail = NULL;


  
}
